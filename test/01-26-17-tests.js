const assert = require('chai').assert
const { sorter } = require('../01-26-17')

describe('Problems', () => {
  it('should return a string stating who the winningest athlete was', () => {

    assert.strictEqual('dogs', 'cats');
  });
});
